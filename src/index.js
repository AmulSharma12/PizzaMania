import ReactDOM from "react-dom";
import App from "./App";
import "./index.css";

ReactDOM.render(
  //as we have to render whole app over there we dont use <h1> html tag.
  <App />,
  document.getElementById("root")
);
